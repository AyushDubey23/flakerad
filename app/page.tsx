import HomeContent from '@/components/flakerad'

export default function Page() {
  return <HomeContent />
}

// The visual modules consume the same fixture-shaped contracts exposed by components/flakerad.tsx.
// Replace the fixture exports with imports from /data once the backend runner is connected.
