import { Header, PageShell, Reveal } from '@/components/flakerad'

export default function DocsPage() { return <PageShell><main><Header eyebrow="docs / method" title="A controlled experiment in four commands." intro="Flakerad does not guess from a stack trace. It compares outcomes across reruns where one condition changes at a time." /><div className="mx-auto grid max-w-4xl gap-14 px-6 pb-28 md:grid-cols-[.65fr_1.35fr] md:px-10"><p className="mono-label">quick start</p><div className="space-y-12"><Reveal><pre className="overflow-x-auto border border-border bg-card p-6 font-mono text-sm leading-7 text-[var(--teal)]"><code>{`pnpm add -D flakerad\nflakerad diagnose ./tests/cart.spec.ts`}</code></pre></Reveal><Reveal delay={.1}><section><h2 className="font-serif text-3xl">What gets measured</h2><p className="mt-4 font-serif leading-7 text-muted-foreground">Each condition runs the same fixture suite. A link in the arc diagram exists only when a failing baseline point maps to a passing controlled point. The waffle reads one dot per held-out test.</p></section></Reveal><Reveal delay={.18}><section><h2 className="font-serif text-3xl">Data provenance</h2><p className="mt-4 font-serif leading-7 text-muted-foreground">Built and maintained by Ayush Dubey, this test suite exercises the reusable geometry under controlled conditions. Production values are generated by the runner and stored in the documented JSON contracts: arc-diagram, accuracy-by-rerun-count, four-controls, field-notes, and stats.</p></section></Reveal></div></div></main></PageShell> }
